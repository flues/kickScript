<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-light">
    <div class="container py-5">
        <h1>{{ title }}</h1>
        
        <div class="card bg-dark text-light border-secondary mb-4">
            <div class="card-header">
                <h2 class="h5 mb-0">Test Ergebnisse</h2>
            </div>
            <div class="card-body">
                <p>Schreiben erfolgreich: <span class="badge {% if writeSuccess %}bg-success{% else %}bg-danger{% endif %}">
                    {{ writeSuccess ? 'Ja' : 'Nein' }}
                </span></p>
                
                <div class="row">
                    <div class="col-md-6">
                        <h3 class="h6">Original-Daten:</h3>
                        <pre class="bg-dark border border-secondary p-3 rounded">{{ originalData|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
                    </div>
                    <div class="col-md-6">
                        <h3 class="h6">Gelesene Daten:</h3>
                        <pre class="bg-dark border border-secondary p-3 rounded">{{ readData|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
                    </div>
                </div>
            </div>
        </div>
        
        <a href="{{ url_for('home') }}" class="btn btn-primary">Zurück zur Startseite</a>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 